language: java

jdk:
  - openjdk8

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

addons:
  sonarcloud:
    organization: "krishnawisely"
    token:
      secure: 9c120bffef4a971eead9931acb4d936d1506548e # encrypted value of your token

script:
  - ./gradlew sonarqube
notifications:
  email:
    recipients:
      - krishna192168@gmail.com
    on_success: never # default: change
    on_failure: always # default: always